@model IEnumerable<StatisticalQualityControl.Models.Sector>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="dashboard_graph">
            <div class="row x_title">
                <div class="col-md-6">
                    <h3>Sektörler</h3>
                </div>
                <div class="col-md-6">
                    <p style="float:right">
                        @Html.ActionLink("Yeni Sektör Ekle", "Create")
                    </p>
                </div>
            </div>
            <div class="col-md-12 col-sm-12 col-xs-12">

                <table class="table">
                    <tr>
                        <th>
                            Sektör Adı
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SectorName)
                            </td>
                            <td>
                                @Html.ActionLink("Güncelle", "Edit", new { id = item.id }) |
                                @Html.ActionLink("Sil", "Delete", new { id = item.id })
                            </td>
                        </tr>
                    }

                </table>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
</div>


