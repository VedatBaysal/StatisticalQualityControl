@model IEnumerable<StatisticalQualityControl.Models.Measurement>

@{
    ViewBag.Title = "Ölçümler";
}
<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="dashboard_graph">
            <div class="row x_title">
                <div class="col-md-6">
                    <h3>Index</h3>
                </div>
                <div class="col-md-6">
                    <p style="float:right">
                        @Html.ActionLink("Yeni Ölçü Ekle", "Create")
                    </p>
                </div>
            </div>
            <div class="col-md-12 col-sm-12 col-xs-12">
                <table class="table">
                    <tr>
                        <th>
                            Ölçümler
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MeasurementName)
                            </td>
                            <td>
                                @Html.ActionLink("Güncelle", "Edit", new { id = item.id }) |
                                @Html.ActionLink("Sil", "Delete", new { id = item.id })
                            </td>
                        </tr>
                    }

                </table>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
</div>

